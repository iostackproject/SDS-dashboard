class Controller:
    """
        Controllers class models.
    """

    def __init__(self, controller_id, name, enabled):
        self.id = controller_id
        self.name = name
        self.enabled = enabled
